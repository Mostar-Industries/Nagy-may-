# Skyhawk ML Service - Production Requirements
# Version: 2.0.0

# Core ML
ultralytics>=8.3.0
torch>=2.0.0
torchvision>=0.15.0
onnxruntime>=1.15.0

# Image processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# API framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0

# HTTP client
requests>=2.31.0
httpx>=0.24.0

# Utilities
python-dotenv>=1.0.0

# Optional: GPU support (uncomment for CUDA)
torch --index-url https://download.pytorch.org/whl/cu118

connexion >= 2.6.0
connexion[swagger-ui] >= 2.6.0
python_dateutil == 2.6.0
setuptools >= 21.0.0
swagger-ui-bundle >= 0.0.2
fastapi
uvicorn
ultralytics
pillow
polars
